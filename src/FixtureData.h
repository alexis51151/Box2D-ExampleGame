#pragma once

#include <SFML/Graphics.hpp>
/*
class FixtureData
{
public:
	FixtureData(Sf::Color color);
	~FixtureData();
	sf::Color getColor();
	setColor(sf::Color color);
	int getDataType();
	//setDataType(int dataType);
private:
	int dataType = 0;
	sf::Color color;
};
*/